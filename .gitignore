# ffmpeg.wasm bundles (copied on postinstall — SEC-05)
apps/web/public/static/ffmpeg/

# Local Codex/Claude worktrees are not project source and may contain generated bundles.
.claude/worktrees/
infra/stack.env
infra/docker-compose.stack.override.local.yml
.env
.env.*
!.env.*.example
*.pem
*.key

# Node
node_modules/
dist/
.next/
.pnpm-store/

# Turbo cache
.turbo/

# TypeScript
*.tsbuildinfo

# Build artefacts
*.tgz
*.tar.gz

# OpenAPI exports (CI / local `export-openapi.ts`)
/openapi.json
/openapi.public.json

# Generated API SDK types (regenerate via `pnpm --filter @tahti/api-client generate`;
# CI regenerates before typecheck/test — do not commit)
packages/api-client/src/schema.d.ts

# Large media — marketing hero assets are tracked under website/
*.mp4
!website/output_vhs.mp4

# E2E screenshot tooling (generated locally)
docs/e2e-screenshots/.seed-output.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Vitest coverage
coverage/
apps/*/coverage/
