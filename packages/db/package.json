{
  "name": "@tahti/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:test": "prisma db push --accept-data-loss",
    "db:studio": "prisma studio",
    "lint": "eslint src --ext .ts --max-warnings 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.4",
    "prisma": "^5.22.0",
    "typescript": "^5.9.3",
    "vitest": "^1.6.1"
  }
}
